import 'package:flutter/material.dart';

class NoteFree extends StatelessWidget {
  const NoteFree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}